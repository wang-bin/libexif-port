libexif-port
============

port for win msvc etc.


How To Build
=============

msvc or icl:
=
set BUILD_DIR=/dir/to/build (optional)

qmake

nmake



*nix or mingw:
=
export BUILD_DIR=/dir/to/build (optional)

qmake

make -j4
